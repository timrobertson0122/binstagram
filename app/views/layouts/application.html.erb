<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
  <title>Binstagram</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to "Binstagram", "", class: "btn btn-lg btn-success", id: "logo" %>
    <div class="nav-item">
      <% if user_signed_in? %>
        <%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: "btn btn-default btn-warning", id: "signout" %>
    </div>
      <% else %>
    <div class="nav-item">
        <%= link_to 'Sign in', new_user_session_path, class: "btn btn-default btn-warning" %>
    </div>
    <div class="nav-item">
        <%= link_to 'Sign up', new_user_registration_path, class: "btn btn-default btn-warning" %>
    </div>
    <div class="nav-item">
        <%= link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook), class: "btn btn-default btn-warning" %>
      <% end %>
    </div> 
  </div>
</header>

<%= notice %>
<%= alert %>

<%= yield %>

</body>
</html>
